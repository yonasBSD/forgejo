The default name for the logfiles have changed. If you expect this filename elsewhere (e.g. in logrotate or scripts), either adjust the filename there or set FILE_NAME = gitea.log in your logging sections.
