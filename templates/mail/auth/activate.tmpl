<!DOCTYPE html>
<html>
<head>
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
	<meta name="format-detection" content="telephone=no,date=no,address=no,email=no,url=no">
</head>

{{$activate_url := printf "%suser/activate?code=%s" AppUrl (QueryEscape .Code)}}
<body>
	<p>{{.locale.Tr "email.hi_username" (.DisplayName|DotEscape)}}</p>
	<br>
	<p>{{.locale.Tr "email.activate_account.text_1" AppName}}</p>
	<br>
	<p>{{.locale.Tr "email.activate_account.text_2" .ActiveCodeLives}}</p>
	<p><a href="{{$activate_url}}">{{$activate_url}}</a></p>
	<p>{{.locale.Tr "email.link_not_working"}}</p>
	<br>
	<p>{{.locale.Tr "email.activate_account.text_3" AppName}}</p>
	<br>
	{{template "mail_tool/footer_simple" .}}
</body>
</html>
